# Product Requirements

Obviously we are using Rust and Dioxus. If you reached this document, then you most likely saw the readme file. What this app should do first is:
1. Show a minimal display to insert any valid task
   1. This action doesn't need to be saved at this time. So the tasks will be saved in memory or state. 
   2. There should be a simple title "Errday" with a catchy tagline
2. After the user clicks next, then, t 